{
  "name": "anote",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "vite:dev": "vite",
    "vite:build": "vite build",
    "raycast:dev": "bun run --cwd raycast dev",
    "raycast:build": "bun run --cwd raycast build",
    "raycast:typecheck": "bun run --cwd raycast typecheck",
    "raycast:test": "bun run --cwd raycast test",
    "test:perf": "bun run --cwd tests perf",
    "test:perf:ui": "bun run --cwd tests perf:ui",
    "test:perf:sample": "bun run --cwd tests perf:sample"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@milkdown/crepe": "^7.18.0",
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/plugin-dialog": "^2"
  }
}
